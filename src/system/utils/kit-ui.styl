/**
 * General UI-related kit utilities (Layout, Color Contrast, etc.)
 */

// UseFlex(ff?: FlexFlow, g?: Unit | TokenOrVar, jc?: FlexJustify, ai?: FlexAlign, ac?: FlexAlign, inline?: boolean, logs?: string): CSS Flexbox
UseFlex(ff = null, g = null, jc = null, ai = null, ac = null, inline = false, logs = '')
  $logs = dsTrace(logs, 'UseFlex', Qs('@received -> "Flex Flow":' ff '"Gap":' g '"Justify Content":' jc '"Align Content":' ac '"Align Items":' ai '"Inline":' inline))
  display: inline ? inline-flex : flex
  if ff
    dsExpectAcceptedOption(ff, 'row' 'row-reverse' 'column' 'column-reverse', $logs)
    flex-flow: Uq(ff)
  if g  
    gap: dsIsString(g) ? dsResolveToVarRef(g, $logs) : Unit(g, logs: $logs)
  if jc
    dsExpectAcceptedOption(jc, 'flex-start' 'flex-end' 'center' 'space-between' 'space-around' 'space-evenly', $logs)
    justify-content: Uq(jc)
  if ai
    dsExpectAcceptedOption(ai, 'flex-start' 'flex-end' 'center' 'stretch' 'baseline', $logs)
    align-items: Uq(ai)
  if ac
    dsExpectAcceptedOption(ac, 'flex-start' 'flex-end' 'center' 'stretch' 'baseline', $logs)
    align-content: Uq(ac)

/*
 * Returns a contrasting color (black or white) against a given base color.
 * Useful for ensuring readability in borders, text, or any property.
 * @param {CSS color | TokenOrVar} againstColor - The base color to contrast against.
 * @param {Factor}? pivotL - Contrasting color will be white for base colors darker than this l.
 * @param {string}? logs - Debugging logs or message
 * @return {CSS color} The contrasting color (oklch expression).
 */
Contrast(againstColor, pivotL = null, logs = '')
  $logs = dsTrace(logs, 'Contrast', Qs('@received -> "Against Color":' againstColor '"Pivot L":' (pivotL == null ? 'default' : pivotL)))
  $pivotL = pivotL == null ? Var('color-control:contrast-pivot-l', logs: $logs) : Factor(pivotL, $logs)
  $baseColor = typeof(againstColor) == 'rgba' ? againstColor : dsResolveToVarRef(againstColor, $logs)
  // Generate a binary contrast color (black or white) by forcing lightness to clamp.
  // The extreme multiplier (-1000) pushes the calculation to the absolute range limits:
  // - Lighter than pivot (currentL > pivotL): Large negative value, clamped to 0 (Black).
  // - Darker than pivot (currentL < pivotL): Large positive value, clamped to 1 (White).
  // Formula: contrastL = (currentL - pivotL) * -1000
  return Oklch('from' $baseColor Calc('(l -' $pivotL ') * -1000') 0.04 'h')

/*
 * Produces a contrasting color and background-color declaration pair.
 * @param {'bg' | 'fg'} against - The base element to contrast against.
 * @param {CSS color | TokenOrVar} againstColor - The color of that base element.
 * @param {Factor}? contrastPivotL - Contrasting color will be white for base colors darker than this l
 * @param {string}? logs - Debugging logs or message
 * @return {CSS} CSS declarations for background-color and color with contrasting values
 */
UseContrast(against, againstColor, contrastPivotL = null, logs = '')
  $logs = dsTrace(logs, 'UseContrast', Qs('@received -> "Against":' against '"Against Color":' againstColor '"Contrast Pivot L":' (contrastPivotL == null ? 'default' : contrastPivotL)))
  dsExpectAcceptedOption(against, 'bg' 'fg', $logs)
  $baseColor = typeof(againstColor) == 'rgba' ? againstColor : dsResolveToVarRef(againstColor, $logs)
  $contrastingColor = Contrast($baseColor, contrastPivotL, logs: $logs)
  if against == 'bg'
    background-color: $baseColor
    color: $contrastingColor
  else if against == 'fg'
    background-color: $contrastingColor
    color: $baseColor
